import { type RunnerName } from './types.js';

export const DEFAULT_RUNNER: RunnerName = 'claude';
export const DEFAULT_BUDGET_USD = 10;
export const DEFAULT_MAX_RETRIES = 3;
export const DEFAULT_TIMEOUT_MS = 30 * 60 * 1000;
